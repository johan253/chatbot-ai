import { Container, Typography } from '@mui/material'

const Navbar = () => {
    return (
        <Container>
            <Typography variant="h3">
                Navbar
            </Typography>
        </Container>
    );
};

export default Navbar;